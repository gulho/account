spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/account
    username: user
    password: password
  rabbitmq:
    host: localhost
    port: 5672
mybatis:
  type-handlers-package: ee.gulho.assignment.account.configuration.typeHander
#  mapper-locations: classpath*:**/mapper/*Mapper.xml